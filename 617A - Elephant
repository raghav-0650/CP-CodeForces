#include <iostream>
using namespace std;

int main() {
    int x;
    cin >> x;
    
    int steps = (x + 4) / 5; // This is equivalent to ceil(x / 5.0)
    
    cout << steps << endl;
    return 0;
}
